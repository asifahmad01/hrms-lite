# Development-only tools — not needed in production
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# ── Linting + formatting ──────────────────────────────────────────────────────
ruff>=0.6.0             # linter + formatter (replaces flake8, isort, black)

# ── Type checking ─────────────────────────────────────────────────────────────
mypy>=1.11.0

# ── Type stubs for packages that need them ─────────────────────────────────────
# SQLAlchemy 2.0+ and Pydantic v2 ship their own stubs — no extras needed.
